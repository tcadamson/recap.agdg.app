{% extends "base.html.jinja" %}

{% block title %}Home{% endblock title %}

{% block content %}
<h1 class="text-center">/agdg/ Weekly Recap</h1>
<div class="content-box">
    <div class="catalog-header text-center">
        <h2><a href="{{url_for("view", datestamp = datestamp)}}">Now: {{datestamp | semantic_datestamp}}</a></h2>
    </div>
</div>
<div class="content-box">
    <p class="text-center">Information:</p>
    <p class="text-raw">
        Each week begins on Monday at 12 AM, UTC.

        :: (Title) ::
        Dev ::
        Tools ::
        Web ::
        (Progress)

        Dev, Tools, and Web are optional fields. If no file is attached to the post, a default image will be used. To update your game's title, use the following syntax:

        :: (Title) -> (New Title) ::
    </p>
</div>
{% endblock content %}
